@model IEnumerable<Eye.Team.Web.ViewModels.TimeExibicaoViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomeCidade)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeCidade)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IdTime }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdTime }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdTime })
            </td>
        </tr>
    }

</table>
